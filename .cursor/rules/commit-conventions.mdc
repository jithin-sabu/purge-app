---
description: Simple, plain-language commit messages (Conventional Commits + SemVer)
alwaysApply: true
---

# Commit Conventions

Follow [awesome-commit-conventions](https://github.com/khasky/awesome-commit-conventions) for structure and versioning. **Write every message so a non-developer could understand it.**

## Primary rule: simple and plain language

Commit messages describe **what changed for the user or the app**, not how the code was changed.

- Use everyday words — avoid jargon, acronyms, and internal names
- Focus on the outcome: what works better, what was broken, what was added
- Keep the summary free of class names, function names, file paths, and framework APIs
- Save technical detail for the optional body, and only when it helps future readers

**Good**

```
fix(settings): stop window flickering when opened
feat(scheduler): add weekly cleaning preference
fix(large-files): show correct size for very large files
```

**Avoid**

```
fix(settings): debounce NSWindow makeKeyAndOrderFront calls
refactor(purge-store): extract ScanResultCache into dedicated actor
fix(ui): update GeneralModeView.onAppear lifecycle hook
```

## Format

```
<type>(<scope>): <short summary>

<body: why the change, what was wrong — still in plain language>

<footer: Closes #123, BREAKING CHANGE:, Co-authored-by:>
```

Only the first line is required. Body and footer are optional.

## Header rules

- Imperative mood: `add`, `fix`, `remove` — not `added`, `adds`, `fixing`
- ≤ ~50 characters, lowercase after `:`, no trailing period
- Wrap body lines at 72 characters

## Types

| Type | Use for |
|------|---------|
| `feat` | New user-facing feature (minor bump) |
| `fix` | Bug fix (patch bump) |
| `docs` | Documentation only |
| `refactor` | Code change, no behavior change |
| `perf` | Performance improvement |
| `test` | Tests |
| `build` | Xcode project, dependencies, release tooling |
| `ci` | CI pipelines |
| `chore` | Housekeeping (version bumps, config) — not user-facing |
| `style` | Formatting only |
| `revert` | Reverts a prior commit |

Breaking changes: `feat(api)!:` or `BREAKING CHANGE:` in footer → major bump.

## Scopes

Use `kebab-case`, stay consistent, match project areas. Examples for this repo:

`menu-bar`, `settings`, `scheduler`, `update-checker`, `about`, `purge-store`, `release`

Global changes may omit scope: `build:`, `ci:`, `chore(release):`.

## Examples

```
feat(scheduler): add weekly cleaning preference
fix(menu-bar): keep popover open during scan
fix(large-files): show correct size for huge files
refactor(purge-store): simplify how scan results are stored
chore(release): bump version to 1.2.4
docs: update security disclosure process
```

Header = **what** (in plain language). Body = **why**. Use `Closes #123` in footer when applicable.
